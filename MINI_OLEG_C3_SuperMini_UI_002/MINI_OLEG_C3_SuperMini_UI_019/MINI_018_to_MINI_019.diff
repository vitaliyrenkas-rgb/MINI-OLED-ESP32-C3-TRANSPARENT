--- /mnt/data/mini019_build/MINI_OLEG_C3_SuperMini_UI_018/MINI_OLEG_C3_SuperMini_UI_018.ino	2026-07-20 19:24:28.000000000 +0000
+++ /mnt/data/mini019_build/MINI_OLEG_C3_SuperMini_UI_019/MINI_OLEG_C3_SuperMini_UI_019.ino	2026-07-20 19:40:10.592941794 +0000
@@ -14,7 +14,7 @@
 
 // ============================================================
 // MINI OLEG — single-screen weather clock
-// Build: MINI-018-INTERNAL-BOLT — GPIO1 battery ADC + integrated battery bolt
+// Build: MINI-019-CLOCK-BOTTOM-ALIGN — internal battery bolt + lower-edge clock alignment
 // Board: ESP32-C3 Super Mini (Tenstar Robot)
 // Display: Waveshare 1.51" Transparent OLED, SSD1309, 128x64
 // Interface: factory 4-wire SPI
@@ -77,7 +77,7 @@
 );
 
 // ---------------- Runtime constants ----------------
-static constexpr char BUILD_VERSION[] = "MINI-017-GPIO1-BATTERY-UI";
+static constexpr char BUILD_VERSION[] = "MINI-019-CLOCK-BOTTOM-ALIGN";
 static constexpr char AP_SSID[] = "MINI-OLEG-SETUP";
 static constexpr char AP_PASSWORD[] = "olegsetup";
 static constexpr char DEVICE_HOSTNAME[] = "mini-oleg";
@@ -715,7 +715,8 @@
   drawCenteredUtf8InBox(weekday, 80, 48, 29, u8g2_font_4x6_t_cyrillic);
 }
 
-static constexpr int BOTTOM_DIGIT_TOP_Y = 35;
+static constexpr int DATE_DIGIT_TOP_Y = 35;
+static constexpr int LOWER_CONTENT_EDGE_Y = 62;
 
 void drawClockBlock(const struct tm* now) {
   String timeText = "--:--";
@@ -735,15 +736,20 @@
 
   u8g2.setFont(u8g2_font_logisoso24_tn);
   const int textWidth = u8g2.getUTF8Width(timeText.c_str());
-  const int baselineY = BOTTOM_DIGIT_TOP_Y + u8g2.getAscent();
+
+  // Align the visible lower edge of the large clock digits with the
+  // lower edge of the month name. This intentionally replaces the old
+  // top-edge alignment, which looked visually uneven on the real OLED.
+  u8g2.setFontPosBottom();
 
   // Reserve the first 12 px for the complete status column and spacing.
   int x = 13;
   if (x + textWidth > 90) x = 90 - textWidth;
   if (x < 1) x = 1;
 
-  u8g2.setCursor(x, baselineY);
+  u8g2.setCursor(x, LOWER_CONTENT_EDGE_Y);
   u8g2.print(timeText);
+  u8g2.setFontPosBaseline();
 }
 
 void drawDateBlock(const struct tm* now) {
@@ -751,10 +757,10 @@
   const String monthText = now ? String(ukrainianMonthGenitive(now->tm_mon)) : "--";
 
   u8g2.setFont(u8g2_font_logisoso18_tn);
-  const int dayBaselineY = BOTTOM_DIGIT_TOP_Y + u8g2.getAscent();
+  const int dayBaselineY = DATE_DIGIT_TOP_Y + u8g2.getAscent();
 
-  // Start 3 px to the right of the divider. The top of the date digits
-  // is aligned exactly with the top of the clock digits.
+  // Start 3 px to the right of the divider. The date keeps its
+  // established geometry; only the clock was lowered for bottom alignment.
   drawCenteredUtf8InBox(dayText, 94, 34, dayBaselineY, u8g2_font_logisoso18_tn);
   drawCenteredUtf8InBox(monthText, 94, 34, 62, u8g2_font_4x6_t_cyrillic);
 }
--- /mnt/data/mini019_build/MINI_OLEG_C3_SuperMini_UI_018/README.md	2026-07-20 19:24:28.000000000 +0000
+++ /mnt/data/mini019_build/MINI_OLEG_C3_SuperMini_UI_019/README.md	2026-07-20 19:40:10.736773168 +0000
@@ -1,23 +1,23 @@
-# MINI OLEG — MINI-018 Internal Battery Bolt
+# MINI OLEG — MINI-019 Clock Bottom Align
 
 ## Статус
 
-Підготовлений drop-in build для першого фізичного тесту на реальному
-ESP32-C3 Super Mini + Waveshare 1.51" Transparent OLED.
+Drop-in build для фізичного тесту на реальному ESP32-C3 Super Mini +
+Waveshare 1.51" Transparent OLED.
 
-**Код ще не проходив hardware test після переходу GPIO1 на батарею.**
-Перший запуск потрібен для перевірки геометрії і калібрування ADC по мультиметру.
+**Код ще не проходив hardware test після цієї геометричної правки.**
 
-## Що змінилось від MINI-017
+## Що змінилось від MINI-018
 
-- окрему блискавку під батареєю прибрано;
-- блискавка перенесена всередину батареї;
-- гліф малюється в XOR-режимі: над порожньою частиною він білий, над
-  зафарбованою частиною автоматично стає чорним;
-- корпус батареї подовжено до `y=62`, тобто до нижнього краю назви місяця;
-- ширина батареї лишилась 9 пікселів — рівно як у Wi-Fi-іконки;
-- великий час посунуто на 2 пікселі вправо: стартова позиція `x=13`;
-- батарейний ADC, фільтрація, Serial-лог і решта MINI-017 не змінювались.
+- красива внутрішня XOR-блискавка в батареї залишена без змін;
+- ширина Wi-Fi та батареї лишилась 9 px;
+- час лишився на `x=13`;
+- великі цифри часу більше не вирівнюються по верхньому краю з числом дати;
+- нижній видимий край часу тепер прив’язаний до `y=62` — тієї самої нижньої
+  лінії, по якій стоїть назва місяця і закінчується корпус батареї;
+- після локального `setFontPosBottom()` режим позиціювання шрифту одразу
+  повертається в `setFontPosBaseline()`, тому решта UI не змінюється;
+- рядок `BUILD_VERSION` виправлено на `MINI-019-CLOCK-BOTTOM-ALIGN`.
 
 ## Актуальне підключення OLED
 
@@ -44,46 +44,25 @@
 BAT-/HU-009 GND ------------- ESP32-C3 GND
 ```
 
-Старий конденсатор/12 kOhm/bias-вузол audio-sense сюди не переноситься.
+Старий audio-sense на GPIO1 не використовується.
 
-## Геометрія статусного стовпчика
+## Геометрія нижнього блоку
 
 ```text
-x = 1..9
-Wi-Fi:             y = 33..42
-battery terminal:  y = 43..44
-battery body:      y = 45..62
-lightning:         усередині battery body, XOR
-clock start:       x = 13
+Wi-Fi width:          9 px
+battery width:        9 px
+clock start:          x = 13
+battery lower edge:   y = 62
+clock lower edge:     y = 62
+month lower line:     y = 62
 ```
 
-Ширина Wi-Fi і батареї — 9 пікселів. Батарею не звужували, щоб не ламати
-узгоджене правило однакової ширини. Додатковий баланс отримано зсувом часу
-на 2 пікселі вправо.
-
-## Перший тест і калібрування
-
-У верхній частині скетча є чотири константи:
-
-```cpp
-static constexpr float BATTERY_VOLTAGE_SCALE = 1.000f;
-static constexpr float BATTERY_VOLTAGE_OFFSET = 0.000f;
-static constexpr float BATTERY_EMPTY_VOLTAGE = 3.30f;
-static constexpr float BATTERY_FULL_VOLTAGE = 4.20f;
-```
+## Перший фізичний тест
+
+Перевірити на живому OLED три речі:
 
-Перший прохід:
+1. Чи низ `13:07` візуально стоїть на одній лінії з низом `липня`.
+2. Чи не став час надто близько до горизонтальної лінії `y=31`.
+3. Чи зберігся комфортний проміжок між батареєю та першою цифрою часу.
 
-1. Виміряти BAT+ мультиметром.
-2. Відкрити Serial Monitor `115200`.
-3. Порівняти мультиметр з рядком:
-   `Battery ADC: ... mV / BAT: ... V / ...%`.
-4. Після реальних значень підкрутити `SCALE/OFFSET`.
-5. Окремо узгодити реальні `EMPTY/FULL` для батареї HU-009.
-
-## Бібліотеки
-
-- U8g2
-- ArduinoJson
-- ESP32 Arduino core (`WiFi`, `HTTPClient`, `WebServer`, `DNSServer`,
-  `Preferences`)
+ADC-калібрування лишається таким самим, як у MINI-018.
